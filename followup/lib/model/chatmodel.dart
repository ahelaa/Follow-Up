class chatmodel {
  String? name;
  String? icon;
  bool? isgroup;
  String? time;
  String? currentmessage;
  chatmodel(
      {this.name, this.icon, this.isgroup, this.currentmessage, this.time});
}